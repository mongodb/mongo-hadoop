1. Download datasets from http://www.crawdad.org/download/dartmouth/campus/snmp/fall03.tar.gz
   Before, you need to register at CRAWDAD for getting username and password.

2. Run buildScript.sh to get script file that can be used to import dataset to MongoDB system.

3. Run splitBigFile.sh because too big script file cannot be imported to MongoDB system.

4. Run importdb.sh to import dataset in form of script file to MongoDB system.

5. Modify original hadoop 2.0 path in build.xml to your hadoop 2.0 path.

6. Compiler MapReduce Funcation by "nice ant"

7. Run "java SnmpStatistic" under "./build/example/snmp/"
